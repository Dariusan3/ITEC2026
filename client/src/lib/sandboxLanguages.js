/** Languages supported by POST /api/run (Docker) — keep in sync with LANG_CONFIG on server. */
export const SANDBOX_RUN_LANGUAGES = new Set([
  "javascript",
  "typescript",
  "python",
  "rust",
  "go",
  "java",
  "c",
])
